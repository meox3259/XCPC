#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int n, m, v, p;
	cin >> n >> m >> v >> p;

	vector<int> a;
	a.resize(n);

	for(int i = 0; i < n; ++i) {
		cin >> a[i];
	}

	sort(a.begin(), a.end(), greater<int> ());

	vector<int> st;

	for(int i = 0; i < n; ++i) {
		if(st.empty()) {
			
		}
	}

	for(int i = 0; i < n; ++i) {
		if(n - i)
	}
	return 0;
}
