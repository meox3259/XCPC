#include <bits/stdc++.h>
using namespace std;
int main() {
	int T; scanf("%d", &T);
	while(T--) {
		scanf("%d%d", &n, &m);
		for(int i = 1; i <= n; ++i) {
			scanf("%d", &a[i]);
		}
		while(m--) {
			char opt[10];
			int l, r, k;
			scanf("%s%d%d", opt, &l, &r);
			if(opt[0] == 'Q') {
				
			}
		}
	}
	return 0;
}
